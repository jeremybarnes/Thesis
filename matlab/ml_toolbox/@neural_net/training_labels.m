function labels = training_labels(obj)

% TRAINING_LABELS return samples neural net is being trained by
%
% SYNTAX:
%
% samples = training_labels(obj)
%
% FIXME: comment

% @neural_net/training_labels.m
% Jeremy Barnes, 2/10/1999
% $Id$

labels = obj.y;
